#include <stdio.h>
#include <stdlib.h>

int n, p, sum;

int main(){
    scanf("%d", &n);
    scanf("%d", &p);
    sum = p * n;
    printf("%d\n", sum);
    system("pause");
    return 0;
}
